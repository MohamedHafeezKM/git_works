git clone remote path (Exact copy from github)

git add -A  (All changes)( from local system to staging area)

git commit -m "your message"  (from staging arae to local repositry)
git push origin main/master   (from local repositry to remote repositry)

